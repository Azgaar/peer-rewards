import React from 'react';
import { Container } from '@material-ui/core';

const MyRewards = (): JSX.Element => {
  return <Container maxWidth="md">MyRewards</Container>;
};

export default MyRewards;
